version: "3.7"

##############
#
# Chatwoot Enterprise Edition v4.3.0
# Imagem: witrocha/chatwit:v4.3.0
# 
# Funcionalidades Enterprise habilitadas:
# - Disable Branding
# - Audit Logs  
# - SLA
# - Captain Integration (AI)
# - Custom Roles
# - Response Bot
#
# O banco será configurado automaticamente na primeira execução
#############
services:
  chatwoot_app:
    image: witrocha/chatwit:v4.3.0
    entrypoint: /app/docker/entrypoints/rails-enterprise.sh
    command: sh -c "bundle exec rails s -p 3000 -b 0.0.0.0"
    volumes:
      - chatwoot_data:/app/storage
      - chatwoot_public:/app
    networks:
      - minha_rede
    environment:
      - INSTALLATION_NAME=${INSTALLATION_NAME}
      - NODE_ENV=${NODE_ENV}
      - RAILS_ENV=${RAILS_ENV}
      - INSTALLATION_ENV=${INSTALLATION_ENV}
      - SECRET_KEY_BASE=${SECRET_KEY_BASE}
      - FRONTEND_URL=${FRONTEND_URL}
      - DEFAULT_LOCALE=${DEFAULT_LOCALE}
      - FORCE_SSL=${FORCE_SSL}
      - ENABLE_ACCOUNT_SIGNUP=${ENABLE_ACCOUNT_SIGNUP}
      - REDIS_URL=${REDIS_URL}
      - POSTGRES_HOST=${POSTGRES_HOST}
      - POSTGRES_USERNAME=${POSTGRES_USERNAME}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DATABASE=${POSTGRES_DATABASE}
      - ACTIVE_STORAGE_SERVICE=local
      - RAILS_LOG_TO_STDOUT=${RAILS_LOG_TO_STDOUT}
      - USE_INBOX_AVATAR_FOR_BOT=${USE_INBOX_AVATAR_FOR_BOT}
      # Configurações de Email
      - MAILER_SENDER_EMAIL=${MAILER_SENDER_EMAIL}
      - SMTP_DOMAIN=${SMTP_DOMAIN}
      - SMTP_ADDRESS=${SMTP_ADDRESS}
      - SMTP_PORT=${SMTP_PORT}
      - SMTP_USERNAME=${SMTP_USERNAME}
      - SMTP_PASSWORD=${SMTP_PASSWORD}
      - SMTP_AUTHENTICATION=${SMTP_AUTHENTICATION}
      - SMTP_ENABLE_STARTTLS_AUTO=${SMTP_ENABLE_STARTTLS_AUTO}
      - SMTP_OPENSSL_VERIFY_MODE=${SMTP_OPENSSL_VERIFY_MODE}
      - MAILER_INBOUND_EMAIL_DOMAIN=${MAILER_INBOUND_EMAIL_DOMAIN}
      # Enterprise Configuration (configurado automaticamente)
      - INSTALLATION_PRICING_PLAN=enterprise
      - INSTALLATION_PRICING_PLAN_QUANTITY=100
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      resources:
        limits:
          cpus: "4.0"
          memory: 4096M

      labels:
        - traefik.enable=true
        - traefik.http.routers.chatwoot_app.rule=Host(`chatwoot.witdev.com.br`)
        - traefik.http.routers.chatwoot_app.entrypoints=websecure
        - traefik.http.routers.chatwoot_app.tls.certresolver=letsencryptresolver
        - traefik.http.routers.chatwoot_app.priority=1
        - traefik.http.routers.chatwoot_app.service=chatwoot_app
        - traefik.http.services.chatwoot_app.loadbalancer.server.port=3000
        - traefik.http.services.chatwoot_app.loadbalancer.passHostHeader=true
        # Cabeçalhos de Segurança
        - traefik.http.middlewares.chatwoot_app_secure.headers.stsSeconds=31536000
        - traefik.http.middlewares.chatwoot_app_secure.headers.stsIncludeSubdomains=true
        - traefik.http.middlewares.chatwoot_app_secure.headers.stsPreload=true
        - traefik.http.middlewares.chatwoot_app_secure.headers.contentTypeNosniff=true
        - traefik.http.middlewares.chatwoot_app_secure.headers.browserXssFilter=true
        - traefik.http.middlewares.chatwoot_app_secure.headers.forceSTSHeader=true
        - traefik.http.middlewares.chatwoot_app_secure.headers.sslRedirect=true
        - traefik.http.routers.chatwoot_app.middlewares=chatwoot_app_secure@docker
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  chatwoot_sidekiq:
    image: witrocha/chatwit:v4.3.0
    command: sh -c "bundle exec sidekiq -C config/sidekiq.yml"
    volumes:
      - chatwoot_data:/app/storage
      - chatwoot_public:/app
    networks:
      - minha_rede
    environment:
      - INSTALLATION_NAME=${INSTALLATION_NAME}
      - NODE_ENV=${NODE_ENV}
      - RAILS_ENV=${RAILS_ENV}
      - INSTALLATION_ENV=${INSTALLATION_ENV}
      - SECRET_KEY_BASE=${SECRET_KEY_BASE}
      - FRONTEND_URL=${FRONTEND_URL}
      - DEFAULT_LOCALE=${DEFAULT_LOCALE}
      - FORCE_SSL=${FORCE_SSL}
      - ENABLE_ACCOUNT_SIGNUP=${ENABLE_ACCOUNT_SIGNUP}
      - REDIS_URL=${REDIS_URL}
      - POSTGRES_HOST=${POSTGRES_HOST}
      - POSTGRES_USERNAME=${POSTGRES_USERNAME}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DATABASE=${POSTGRES_DATABASE}
      - ACTIVE_STORAGE_SERVICE=local
      - RAILS_LOG_TO_STDOUT=${RAILS_LOG_TO_STDOUT}
      - USE_INBOX_AVATAR_FOR_BOT=${USE_INBOX_AVATAR_FOR_BOT}
      # Configurações de Email
      - MAILER_SENDER_EMAIL=${MAILER_SENDER_EMAIL}
      - SMTP_DOMAIN=${SMTP_DOMAIN}
      - SMTP_ADDRESS=${SMTP_ADDRESS}
      - SMTP_PORT=${SMTP_PORT}
      - SMTP_USERNAME=${SMTP_USERNAME}
      - SMTP_PASSWORD=${SMTP_PASSWORD}
      - SMTP_AUTHENTICATION=${SMTP_AUTHENTICATION}
      - SMTP_ENABLE_STARTTLS_AUTO=${SMTP_ENABLE_STARTTLS_AUTO}
      - SMTP_OPENSSL_VERIFY_MODE=${SMTP_OPENSSL_VERIFY_MODE}
      - MAILER_INBOUND_EMAIL_DOMAIN=${MAILER_INBOUND_EMAIL_DOMAIN}
      # Enterprise Configuration
      - INSTALLATION_PRICING_PLAN=enterprise
      - INSTALLATION_PRICING_PLAN_QUANTITY=100
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      resources:
        limits:
          cpus: "4.0"
          memory: 4096M

    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  chatwoot_data:
    driver: local
    driver_opts:
      type: "nfs"
      o: "${NFS_SERVER}"
      device: ":/mnt/dockervolumes/chatwoot_data"
  chatwoot_public:
    driver: local
    driver_opts:
      type: "nfs"
      o: "${NFS_SERVER}"
      device: ":/mnt/dockervolumes/chatwoot_public"

networks:
  minha_rede:
    external: true
    name: minha_rede 